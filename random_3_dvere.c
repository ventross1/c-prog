#include <stdlib.h>
#include <stdio.h>
#include <math.h>
#include <time.h>
//srand(time(NULL));
int main(){
    int i = 0;
    srand(time(NULL));
    char d[] = {0, 0, 1};
    int w[3];
    w[i] = d[rand() % 3];
    for(i = 0; i = 3; i++){
    printf("%i", w[i]);
    if(w[i] = 1){
        d[4] = 0;}
    {
        w[i] = d[rand() % 3];}
    }
}